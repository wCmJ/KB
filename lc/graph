pro399除法求值:
    - 题目描述：
        '[["a","c"],["b","a"],["a","e"],["a","a"],["x","x"]]'
        给出方程式 A / B = k, 其中 A 和 B 均为用字符串表示的变量， k 是一个浮点型数字。根据已知方程式求解问题，并返回计算结果。如果结果不存在，则返回 -1.0。
        输入总是有效的。你可以假设除法运算中不会出现除数为 0 的情况，且不存在任何矛盾的结果。
    - 分析：
        1. 由于不要求最小值，所求结果是求出从起点到终点经过路径的乘积，
        2. 用dfs寻找符合条件的路径 
        3. 用哈希表存储一个string到其它有关系string的值，为了在回溯的时候保持原值，在value中增加一个bool变量
        4. 为了防止重复遍历，再用一个哈希表存储string是否被遍历过


