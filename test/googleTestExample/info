测试：
    - 独立，重复
    - 移植，复用
    - 失败时提供多的故障信息
    - 快

Google C++ style:
    - .cc对应.h文件，除main、.inc、单元测试文件之外
        ？？？如果某函数模板为所有相关模板参数显式实例化，或本身就是某类的一个私有成员，那么它就只能定义在实例化该模板的 .cc 文件里。
    - #ifndef PROJECT_PATH_H_
    - 用#include头文件替换前置声明
    - 内联函数：少于10行，简单无循环
    - #include路径及顺序
        1. 相关头文件
        2. C库
        3. C++库
        4. 其他库的.h
        5. 本项目内的.h

    - 在.cc中使用匿名空间或static声明。禁止使用using，内联命名空间。 防止命名冲突
        内联命名空间会自动把内部的标志符放到外面：X::Y::foo()与X::foo()彼此可替代。
        内联命名空间主要用来保持跨版本的ABI兼容性
    





